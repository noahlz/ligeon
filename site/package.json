{
  "name": "ligeon-site",
  "version": "1.0.0",
  "description": "Ligeon project landing page",
  "type": "module",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve"
  },
  "dependencies": {
    "@11ty/eleventy": "^3"
  }
}
